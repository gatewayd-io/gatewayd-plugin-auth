# Example credentials file for gatewayd-plugin-auth.
# Copy this file to credentials.yaml and modify as needed.

users:
  - username: alice
    password: "s3cret_alice"
    auth_methods: ["scram-sha-256", "md5", "cleartext"]
    roles: ["admin"]
    databases: ["mydb", "analytics"]
    enabled: true

  - username: bob
    password: "b0b_password"
    auth_methods: ["md5"]
    roles: ["readonly"]
    databases: ["mydb"]
    enabled: true

  - username: service_account
    password: "svc_p@ss"
    auth_methods: ["md5", "cleartext"]
    roles: ["readwrite"]
    databases: []  # empty = all databases
    enabled: true

  - username: disabled_user
    password: "not_used"
    roles: ["readonly"]
    enabled: false
